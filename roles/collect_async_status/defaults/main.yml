---

cas_async_retries: "{{ gitlab_configuration_async_retries | default(50) }}"
cas_async_delay: "{{ gitlab_configuration_async_delay | default(1) }}"
cas_secure_logging: "{{ gitlab_configuration_secure_logging | default('false') }}"

...